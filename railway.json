{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "dockerfilePath": "backend/Dockerfile/dockerfile",
    "context": "."
  },
  "deploy": {
    "startCommand": "sh -c 'uvicorn app.main:app --host 0.0.0.0 --port $PORT'",
    "healthcheckPath": "/health",
    "env": {
      "PYTHONPATH": "/app/backend"
    }
  }
}